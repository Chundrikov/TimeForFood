spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/consumers
    username: postgres
    password: root
    driver-class-name: org.postgresql.Driver
  liquibase:
    change-log: changelog/db.changelog-master.yml
server:
  port: 8081