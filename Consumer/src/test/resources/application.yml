spring:
  datasource:
    username: sa
    password: sa
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
  liquibase:
    change-log: changelog/db.changelog-test.yml
  flyway:
    enabled: false